<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        A window that shows nothing at all. @android:style/Theme.NoDisplay cannot be used
        here: an activity with that theme never receives the window focus, and without the
        focus Android refuses to hand out the clipboard (API 29 and above).
    -->
    <style name="Theme.OnlyShare" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
